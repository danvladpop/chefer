# Mobile Parity Backlog

> **Purpose:** the ledger of user-facing changes that landed on web while the affected
> feature did **not yet exist** on mobile (`apps/mobile`). Governed by the
> **Platform Parity** section of [`CLAUDE.md`](./CLAUDE.md). Porting agents executing
> [`mobile_native_plan.md`](./mobile_native_plan.md) Wave 2 **must** check this file for
> their feature and mark entries done in the same PR that ports them.
>
> **Baseline:** entries are only needed for changes made **after 2026-08-30** (the date
> `mobile_native_plan.md` was created). Anything older is already covered by the plan's
> rule "port from the current web source" — the web code itself is the spec.

## How to add an entry (for the agent making a web-only change)

Append a row to the table below in the **same PR** as the web change. Keep it one line;
the porting agent will read the web source for details — the entry's job is to make sure
the change is _noticed_, and to capture anything **not discoverable from the web source**
(e.g. "also applies to the mobile-only scan flow", "API added optional field X for this").

## How to drain an entry (for the porting agent)

When porting the feature (or when the feature already exists on mobile and you're
back-filling), implement the change on mobile, verify per `mobile_native_plan.md` §4, and
change the entry's Status to `done (<commit>)` in the same PR. Do not delete rows.

## Ledger

| Date       | Feature (web dir)                                                          | Change (one line)                                                                                                                                                                                                                                                                                      | Web commit/PR                                                                                                                  | Non-obvious notes for mobile                                                                                                                                                                                                                                 | Status                                                                                   |
| ---------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| 2026-09-23 | features/meal-plan (**reverse: mobile → web**)                             | Per-meal replace opens a recipe-picker sheet (own/favourites first, search, pick any recipe via `mealPlan.replaceRecipe`; AI regen as premium footer action) — landed on MOBILE first, web meal-plan page still has no per-meal replace picker                                                         | mobile `apps/mobile/src/features/meal-plan/recipe-picker-sheet.tsx`                                                            | `replaceRecipe` is un-gated (any tier); web already calls it from the tracker RebalanceBanner — reuse that pattern                                                                                                                                           | done (web ReplaceMealSheet, 2026-09-23; `buildPickerSections` lifted to `@chefer/utils`) |
| 2026-09-24 | features/gym (**reverse: mobile → web**, whole feature)                    | Gym mode (Food/Gym switch, setup, Today, active workout, routine editor, exercise library, stats, dashboard card) ships on MOBILE first by owner decision (gym_plan.md D7); web gets it in wave G5                                                                                                     | mobile `apps/mobile/app/(gym)/*`, `app/gym/*`                                                                                  | API complete and platform-neutral; reuse `@chefer/utils` engine + `workoutReducer` + outbox core with a localStorage adapter; see gym_plan.md §7 G5                                                                                                          | in progress (G5)                                                                         |
| 2026-09-24 | nav (**reverse: mobile → web**)                                            | Food / Gym mode switch (gym_plan.md D3): a `Food \| Gym` segmented control in every tab-root header swaps the tab bar to Today / Routine / Exercises / Stats; the mode persists and the app reopens in it — landed on MOBILE first (G1-C, placeholders only), web sidebar + bottom nav do not swap yet | mobile `apps/mobile/src/features/gym/components/mode-switch.tsx`, `app/(gym)/_layout.tsx`                                      | Owner-scoped mobile-first (D7): web lands in G5. First switch to Gym without a gym profile opens Setup (`gym.bootstrap.profile === null`)                                                                                                                    | in progress (G5)                                                                         |
| 2026-09-26 | (auth)/forgot-password + features/auth/components/forgot-password-form.tsx | Forgot password: "Forgot password?" on Sign in → request a reset link (`auth.requestPasswordReset`), same always-success confirmation (audit F-M-AUTH-3-1)                                                                                                                                             | web P0-6 (2026-08-21)                                                                                                          | Emailed link stays the web URL (works in any phone browser)                                                                                                                                                                                                  | done (feat/mobile-auth-parity)                                                           |
| 2026-09-26 | (auth)/reset-password + features/auth/components/reset-password-form.tsx   | Reset password: new + confirm password from a reset token (`auth.resetPassword`), then back to Sign in (audit F-M-AUTH-3-1)                                                                                                                                                                            | web P0-6 (2026-08-21)                                                                                                          | In-app screen opens from the deep link `chefer://reset-password?token=…` (`chefer-dev://` in dev); no manual token entry, same as web                                                                                                                        | done (feat/mobile-auth-parity)                                                           |
| 2026-09-26 | (auth)/register, (auth)/login                                              | Register confirm-password field + Show/Hide password toggle on login and register (rules from `@chefer/types` `auth.ts`)                                                                                                                                                                               | web register-form.tsx / login-form.tsx                                                                                         | Mobile register keeps first name optional and has no last name (unchanged)                                                                                                                                                                                   | done (feat/mobile-auth-parity)                                                           |
| 2026-09-26 | features/tracker (QuickAddSheet)                                           | Quick add (name + kcal, free for all tiers) via `tracker.logCustomMeal` — ported to mobile tracker as a bottom sheet; mobile also offers meal slot + optional macros                                                                                                                                   | web `features/tracker/components/QuickAddSheet.tsx`                                                                            | Validation shared as `parseQuickAdd` in `@chefer/utils` (mirrors API bounds); device-local `localDateStr` date                                                                                                                                               | done (feat/mobile-tracker-parity)                                                        |
| 2026-09-26 | features/meal-plan (RebalanceBanner) + tracker/lib/rebalance-storage       | Week-rebalance banner with Undo (audit TRK-3) — ported: every mobile log surface records `rebalance`, banner on tracker, cook finish and Plan tab                                                                                                                                                      | web `features/meal-plan/components/RebalanceBanner.tsx`                                                                        | Mobile persists in SQLite KV and MERGES swaps (`mergePendingRebalance`, `@chefer/utils`)                                                                                                                                                                     | done (feat/mobile-tracker-parity)                                                        |
| 2026-09-26 | features/recipe (StarRatingWidget)                                         | Star rating + "who liked it" chips + notes via `recipe.rate` / `recipe.getMyRating` — ported to mobile recipe detail (when opened from a Plan day) and the cook-mode finish after logging                                                                                                              | web `features/recipe/components/StarRatingWidget.tsx`                                                                          | Liked-by helpers moved to `@chefer/utils` `rating.ts` (web `liked-by.ts` re-exports); 44pt stars, "Rate N stars" labels                                                                                                                                      | done (feat/mobile-tracker-parity)                                                        |
| 2026-09-26 | features/tracker + meal-plan (**reverse: mobile → web**)                   | Rebalance feedback on the LOGGING surface (tracker, quick add, scan, cook finish — web only shows it on the meal-plan page, F-TRK-3-2) and MERGED pending swaps (web's `handleRebalanceResult` overwrites, so a second rebalance destroys the first undo)                                              | mobile `apps/mobile/src/features/tracker/rebalance-store.ts`, `rebalance-banner.tsx`                                           | Swap web `rebalance-storage.ts` pure helpers for `@chefer/utils` `rebalance.ts` (`mergePendingRebalance`, `parsePendingRebalance`) and render `RebalanceBanner` on the tracker + cook finish                                                                 | done (fix/web-rebalance-feedback)                                                        |
| 2026-09-26 | app/(dashboard)/progress                                                   | Progress charts ported to mobile (audit TRK-4): 28-day calories vs target, macro stack, 90-day weight chart, log form, weigh-in edit/delete; linked from More and the dashboard weight card. Weight change is now goal-aware on web too (F-TRK-4-1, shared `weightChangeTone`)                         | —                                                                                                                              | Charts are the in-house ui-mobile LineChart/BarChart (react-native-svg; LineChart gained `reference` + `xDomain`); weight x-axis is a time scale                                                                                                             | done (feat/mobile-progress-history)                                                      |
| 2026-09-26 | app/(dashboard)/history/[planId]                                           | History plan detail ported to mobile (audit F-M-PAR-1): read-only week, day chips, meals open the recipe; Restore on list + detail now confirms first and spins only its own row (F-M-PREM-1-1)                                                                                                        | —                                                                                                                              | `history.tsx` moved to `history/index.tsx`; getById carries no status, so the list passes `status` as a route param to hide Restore on the ACTIVE week                                                                                                       | done (feat/mobile-progress-history)                                                      |
| 2026-09-26 | tailwind config + globals.css (**reverse: mobile → web**)                  | Motion/depth tokens live in the new zero-dependency `@chefer/tokens` (duration, easing, springs + CSS `linear()` springs, elevation, radius) — mobile consumes them; web Tailwind does not yet                                                                                                         | mobile `packages/tokens`, `packages/ui-mobile/src/motion/motion.ts`                                                            | motion-system.md §2.2 has the ready `tailwind.config.ts` snippet (`transitionDuration`, `transitionTimingFunction`, `boxShadow: var(--elevation-*)`, `borderRadius` roles) + `--elevation-*` CSS vars; `cubicBezier`/`easingFn` exist for a web `useCountUp` | done (feat/web-motion-ports)                                                             |
| 2026-09-26 | packages/ui Button/Chip + raw buttons (**reverse: mobile → web**)          | MO-01 press feedback: mobile `Button`, `Chip`, `Stepper`, `ValueStepper`, `SegmentedControl` scale to 0.97 on press-in (instant) and spring back (snappy); cards 0.98; selection haptic on chips/segments/steppers                                                                                     | mobile `packages/ui-mobile/src/motion/pressable-scale.tsx`                                                                     | Web: `active:scale-[0.97]` + `duration-instant ease-standard` in `buttonVariants`, `hoverOnlyWhenSupported`, then migrate raw `<button>`s (spec phase 0); `navigator.vibrate` only for tick/warning                                                          | done (feat/web-motion-ports)                                                             |
| 2026-09-26 | features/dashboard nutrition-summary (**reverse: mobile → web**)           | MO-06: calorie ring animates from its previous value with a count-up in the centre; past 100% the ring turns amber-600 with an overflow lap and macro bars get the over colour + end cap; bars animate `scaleX`                                                                                        | mobile `apps/mobile/src/features/dashboard/components/nutrition-summary.tsx`, ui-mobile `ProgressRing`/`ProgressBar`/`CountUp` | Web ring is static and caps at 100%; its macro bars use `transition-all` on width (replace with `scaleX` + `origin-left`). Mobile's ring replaced the M2-1 calorie bar (owner decision 3)                                                                    | done (feat/web-motion-ports)                                                             |
| 2026-09-26 | packages/ui Sheet/Drawer (**reverse: mobile → web**)                       | MO-02: mobile `Sheet` fades the scrim and slides the panel (spring gentle in, 220 ms exit-eased out) and calls `onClose` only after the exit; reduced motion → 150 ms crossfade. Meal-plan recipe picker + week summary moved onto it                                                                  | mobile `packages/ui-mobile/src/components/sheet.tsx`                                                                           | Web Sheet/Drawer still unmount instantly on close: add `usePresence(open, exitMs)` with the `setTimeout` fallback (spec MO-02). Drag-to-dismiss waits for RNGH on mobile (native build)                                                                      | done (feat/web-motion-ports)                                                             |
| 2026-09-26 | features/feedback (FeedbackDialog)                                         | Feedback textarea gets a label and a live character counter ("123 / 2,000", "Limit reached"); it used to stop silently at the API's 2,000-char cap (audit F-PROF-2-2)                                                                                                                                  | fix/web-a11y-sweep                                                                                                             | Mobile `FeedbackCard` in `app/(food)/more.tsx` has no `maxLength` and no counter; the API rejects > 2,000 (`feedback.router.ts`)                                                                                                                             | done (fix/mobile-parity-drain)                                                           |
| 2026-09-26 | features/meal-plan (WeekTemplates / My weeks)                              | Copy: "rotate" implied automatic rotation that doesn't exist → "Save a week you like and reuse it. The week you follow repeats each week until you switch." (audit F-PLAN-5-3)                                                                                                                         | fix/web-a11y-sweep                                                                                                             | Mobile `app/my-weeks.tsx` still says "Your Rotation" / "rotate through up to {MAX_TEMPLATES}"                                                                                                                                                                | done (fix/mobile-parity-drain)                                                           |
| 2026-09-26 | features/premium (PostUpgradeActivation)                                   | Post-upgrade "You're premium" sheet is source-aware (household source → "Add your table") and never links a user with a profile to `/onboarding` (F-PREM-1-5, F-PM-9, backlog P2-3)                                                                                                                    | feat/household-audience                                                                                                        | Mobile has no post-upgrade activation sheet at all (profile Upgrade just flips the tier); step logic is pure in web `features/premium/lib/activation-steps.ts` — lift to `@chefer/utils` when porting                                                        | done (fix/mobile-parity-drain)                                                           |
| 2026-09-26 | features/preferences (household-section)                                   | Household members are editable (name, portion, kid, allergies, restrictions) on every tier; free ghost reflects the chip tapped (F-PM-12, backlog P2-3)                                                                                                                                                | feat/household-audience                                                                                                        | Mobile `HouseholdEditor` adds + removes (with confirm) on every tier but has no EDIT of an existing member and no ghost teaser (it shows the scaling upsell card instead); `household.update` is protected on every tier                                     | done (fix/mobile-parity-drain)                                                           |
| 2026-09-26 | features/recipes (cook-mode)                                               | Cook mode defaults its servings to the table's portions for premium households (`useHousehold().portionSum`, backlog P2-3)                                                                                                                                                                             | feat/household-audience                                                                                                        | Mobile cook mode has no servings scaler yet; the mobile recipe screen already defaults to the table via `src/hooks/use-household.ts`                                                                                                                         | done (fix/mobile-parity-drain)                                                           |
