/** Wave-0 placeholder body; G1-A replaces every call site. */
export function notImplemented(name: string): never {
  throw new Error(`@chefer/utils gym: ${name} is not implemented yet (gym_plan.md G1-A)`);
}
