# Gym dogfooding feedback (G3)

Owner feedback from real use of the gym feature. **Collect only.** Items are
triaged and implemented later, in a batch (see `gym_plan.md` §7 G3).

| #   | Date       | Area                                                                  | Feedback (owner's words, lightly edited)                                                                                                        | Notes for triage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Status |
| --- | ---------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| 1   | 2026-09-25 | Setup → other programs                                                | For 3 days/week, offer a **2× upper + 1× lower** split (Upper A / Lower / Upper B) among the "other programs" alternatives.                     | Today 3 days only offers Full Body 3× (beginner/intermediate). Needs new templates in `packages/types/src/gym/templates.ts` (a beginner and an intermediate variant; keep weekly sets per muscle within research §2.2, noting that legs get 1×/week frequency), plus an alternative entry in `recommendTemplate`. Shows on mobile and web via the shared engine.                                                                                                                                                                                                                                                                           | open   |
| 2   | 2026-09-25 | Forms / keyboard (setup → Starting weights; likely other screens too) | While typing into a field, the number keyboard covers the bottom of the screen, so you can't see the field you're typing in or what comes next. | This is keyboard avoidance. The setup wizard's weights list (`apps/mobile/src/features/gym/setup/setup-wizard.tsx`) and other scrolling forms need `KeyboardAvoidingView` (iOS padding) or `react-native-keyboard-controller`-style scroll-to-focused-input. Give numeric inputs a "Next" return key that focuses the next field, and add bottom padding the height of the keyboard. Audit: starting weights, custom exercise form, settings inventory, and routine editor name fields. Check on both iOS and Android (Android `softwareKeyboardLayoutMode`).                                                                              | open   |
| 3   | 2026-09-25 | App shell / status bar (production iPhone build)                      | The time, Wi-Fi and battery at the top aren't visible; it looks as if something white is drawn over them.                                       | The status bar is probably invisible: white/light icons on a white background, or content drawn over the safe area. Check the `expo-status-bar` style in the root layout (should be `dark` on light screens) and `userInterfaceStyle` / edge-to-edge behaviour in the production variant. **Likely root cause:** the root layout uses `<StatusBar style="auto" />`. With the phone in dark mode that gives light icons, while the app is light-only (dark mode is deferred), so they're white on white. Fix: `style="dark"` until dark mode ships. Verify on the device's production build. The screenshot shows it on the Food dashboard. | fixed  |
| 4   | 2026-09-25 | Food/Gym mode switch                                                  | The toggle should be a bit smaller and animated a little (a sliding selection).                                                                 | `SegmentedControl` (`packages/ui-mobile/src/components/segmented-control.tsx`): add a compact size for the header and a reanimated sliding indicator (reanimated is installed). Keep a 44pt hit area even if the visual gets smaller (CLAUDE.md). Mirror it on the web header switch.                                                                                                                                                                                                                                                                                                                                                      | fixed  |
| 5   | 2026-09-25 | Header, right side                                                    | The right side of the header feels empty; put a profile icon or something similar there.                                                        | In dev builds the Tools bubble filled that corner, which hid the gap. Add a profile avatar (initials, like the web's "AJ" circle) at top right on the tab-root headers in both modes, opening Profile/More. This also balances the Food/Gym switch on the left.                                                                                                                                                                                                                                                                                                                                                                            | fixed  |
