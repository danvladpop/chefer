import { GymSettingsScreen } from '../../src/features/gym/settings/settings-screen';

// Gym stack route (G2-B): units, equipment, reminders, pause, sync status.
export default function GymSettingsRoute() {
  return <GymSettingsScreen />;
}
