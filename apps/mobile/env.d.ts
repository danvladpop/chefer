// Committed stand-in for the gitignored, Expo-generated expo-env.d.ts: it
// carries the same expo/types reference so `process.env` stays typed in CI,
// where `expo start` has never run. Duplicate references are harmless locally.
/// <reference types="expo/types" />
