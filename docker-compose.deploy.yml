# Production deployment — single always-on VM (Oracle Always Free), single origin.
#
#   Caddy (TLS + path routing)  ──►  web (Next.js SSR)  :3000
#                               ──►  api (Express+tRPC, workers, SSE, uploads) :3001
#                                    api ──► postgres :5432 (internal only)
#
# Usage (from repo root on the VM):
#   docker compose --env-file .env.production -f docker-compose.deploy.yml up -d --build
# (or just run infrastructure/scripts/deploy.sh)

services:
  postgres:
    image: postgres:16-alpine
    container_name: chefer-postgres
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - chefer

  api:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.api
    image: chefer-api:latest
    container_name: chefer-api
    restart: always
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: '3001'
      HOST: 0.0.0.0
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - uploads:/app/uploads
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:3001/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - chefer

  web:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.web
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
        NEXT_PUBLIC_APP_NAME: ${NEXT_PUBLIC_APP_NAME}
    image: chefer-web:latest
    container_name: chefer-web
    restart: always
    environment:
      NODE_ENV: production
      PORT: '3000'
      # SSR calls the API over the internal network (no public round-trip)
      API_INTERNAL_URL: http://api:3001
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:3000/api/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - chefer

  caddy:
    image: caddy:2-alpine
    container_name: chefer-caddy
    restart: always
    ports:
      - '80:80'
      - '443:443'
    environment:
      PUBLIC_DOMAIN: ${PUBLIC_DOMAIN}
    volumes:
      - ./infrastructure/docker/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web
      - api
    networks:
      - chefer

volumes:
  pgdata:
    name: chefer_pgdata
  uploads:
    name: chefer_uploads
  caddy_data:
    name: chefer_caddy_data
  caddy_config:
    name: chefer_caddy_config

networks:
  chefer:
    name: chefer
    driver: bridge
